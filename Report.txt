I want you to act as a Senior Full-Stack Engineer, DevOps Engineer, Technical Architect, and Coding Mentor.

I am building a portfolio project called:

SPACEOPS MONITORING DASHBOARD

Project Goal:
Develop a production-style full-stack application that simulates monitoring and visualization of satellite ground station telemetry and operational metrics.

Tech Stack:

Frontend:

* Angular (latest stable version)
* TypeScript
* Angular Material
* RxJS

Backend:

* Python FastAPI
* SQLAlchemy
* PostgreSQL

DevOps:

* Docker
* Docker Compose
* GitHub Actions

Version Control:

* Git
* GitHub

Project Requirements:

Dashboard should display:

* Station Name
* CPU Usage
* Memory Usage
* Signal Strength
* Active Connections
* Ground Station Status
* Last Telemetry Timestamp

Backend should expose REST APIs:

GET /stations
GET /stations/{id}
GET /metrics
GET /health

Database should store telemetry records.

Use mock telemetry data generation.

Frontend should include:

* Dashboard Page
* Station Details Page
* Metrics Cards
* Responsive Layout
* Loading States
* Error Handling

Authentication is NOT required.

IMPORTANT RULES:

1. Give FULL code files.
2. Never give placeholders.
3. Never say "implement this yourself".
4. Never omit any code.
5. Always provide exact filenames.
6. Always provide exact folder structure.
7. Always provide terminal commands.
8. Always provide copy-paste-ready code.
9. Build the project incrementally.
10. Wait for me after each major step.

For every step provide:

* Objective
* Folder structure
* Terminal commands
* Full code files
* Expected output
* Verification steps

Development Workflow:

STEP 1:
Create project folder structure.

STEP 2:
Create FastAPI backend.

STEP 3:
Create PostgreSQL integration.

STEP 4:
Create mock telemetry generator.

STEP 5:
Create Angular frontend.

STEP 6:
Build dashboard UI.

STEP 7:
Integrate frontend with backend.

STEP 8:
Create Dockerfiles.

STEP 9:
Create docker-compose.yml.

STEP 10:
Create GitHub Actions CI pipeline.

STEP 11:
Create README.md.

STEP 12:
Prepare GitHub repository.

STEP 13:
Provide Git commands to push to GitHub.

GitHub Repository Name:
spaceops-monitoring-dashboard

README must include:

* Architecture diagram
* Features
* Tech stack
* Screenshots section
* Local setup instructions
* Docker setup instructions
* CI/CD workflow

When generating code:

* Follow industry best practices.
* Use clean architecture where reasonable.
* Include comments.
* Use realistic telemetry data.
* Ensure application runs successfully.

Start with STEP 1 only.
Wait for my confirmation before proceeding to the next step.

FOLDER STRUCTURE 
spaceops-monitoring-dashboard/
│
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   └── v1/
│   │   │       ├── endpoints/
│   │   │       └── __init__.py
│   │   │
│   │   ├── core/
│   │   │   └── __init__.py
│   │   │
│   │   ├── db/
│   │   │   └── __init__.py
│   │   │
│   │   ├── models/
│   │   │   └── __init__.py
│   │   │
│   │   ├── schemas/
│   │   │   └── __init__.py
│   │   │
│   │   ├── services/
│   │   │   └── __init__.py
│   │   │
│   │   ├── telemetry/
│   │   │   └── __init__.py
│   │   │
│   │   ├── main.py
│   │   └── __init__.py
│   │
│   ├── tests/
│   │   └── __init__.py
│   │
│   ├── requirements.txt
│   ├── .env
│   └── .gitignore
│
├── frontend/
│   └── (Angular application will be generated here)
│
├── docker/
│   ├── backend/
│   │   └── Dockerfile
│   │
│   └── frontend/
│       └── Dockerfile
│
├── .github/
│   └── workflows/
│       └── ci.yml
│
├── docs/
│   ├── architecture/
│   ├── screenshots/
│   └── diagrams/
│
├── scripts/
│
├── docker-compose.yml
├── README.md
├── .gitignore
└── LICENSE


mkdir -p spaceops-monitoring-dashboard

cd spaceops-monitoring-dashboard

mkdir -p backend/app/api/v1/endpoints
mkdir -p backend/app/core
mkdir -p backend/app/db
mkdir -p backend/app/models
mkdir -p backend/app/schemas
mkdir -p backend/app/services
mkdir -p backend/app/telemetry
mkdir -p backend/tests

mkdir -p frontend

mkdir -p docker/backend
mkdir -p docker/frontend

mkdir -p .github/workflows

mkdir -p docs/architecture
mkdir -p docs/screenshots
mkdir -p docs/diagrams

mkdir -p scripts

touch backend/app/__init__.py
touch backend/app/main.py

touch backend/app/api/v1/__init__.py
touch backend/app/api/v1/endpoints/__init__.py

touch backend/app/core/__init__.py
touch backend/app/db/__init__.py
touch backend/app/models/__init__.py
touch backend/app/schemas/__init__.py
touch backend/app/services/__init__.py
touch backend/app/telemetry/__init__.py

touch backend/tests/__init__.py

touch backend/requirements.txt
touch backend/.env
touch backend/.gitignore

touch docker/backend/Dockerfile
touch docker/frontend/Dockerfile

touch .github/workflows/ci.yml

touch docker-compose.yml
touch README.md
touch LICENSE
touch .gitignore

Initial Root .gitignore

Filename

.gitignore

Full Content

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/

# Environment
.env

# PostgreSQL
postgres_data/

# Angular
frontend/node_modules/
frontend/dist/

# IDE
.vscode/
.idea/

# Coverage
.coverage
htmlcov/

# Docker
docker-volumes/
Backend .gitignore

Filename

backend/.gitignore

Full Content

__pycache__/
*.pyc
*.pyo
*.pyd

.env

venv/
.venv/
Placeholder requirements.txt

Filename

backend/requirements.txt

Full Content

Leave empty for now.

Placeholder .env

Filename

backend/.env

Full Content

Leave empty for now.

Expected Output

Running:

tree -L 4

Should show a structure similar to:

spaceops-monitoring-dashboard
├── backend
├── frontend
├── docker
├── docs
├── scripts
├── .github
├── docker-compose.yml
├── README.md
├── LICENSE
└── .gitignore
Verification Steps
Verify project root
pwd

Expected:

.../spaceops-monitoring-dashboard
Verify backend structure
tree backend

Expected directories:

app
tests
requirements.txt
.env
.gitignore
Verify docker structure
tree docker

Expected:

docker
├── backend
│   └── Dockerfile
└── frontend
    └── Dockerfile
Verify GitHub workflow folder
tree .github

Expected:

.github
└── workflows
    └── ci.yml
STEP 1 Complete

At this point you should have the complete repository skeleton for spaceops-monitoring-dashboard.

***** STEP 2 ** 

STEP 2 — Create FastAPI Backend Foundation
Objective

Build a production-style FastAPI backend that provides:

FastAPI application startup
Configuration management
API versioning
Health endpoint
Stations endpoint
Metrics endpoint
Pydantic schemas
Mock data service
OpenAPI documentation

Target APIs:

GET /health
GET /api/v1/stations
GET /api/v1/stations/{id}
GET /api/v1/metrics
Updated Backend Structure

After this step:

backend/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── endpoints/
│   │       │   ├── health.py
│   │       │   ├── stations.py
│   │       │   └── metrics.py
│   │       │
│   │       └── api.py
│   │
│   ├── core/
│   │   └── config.py
│   │
│   ├── schemas/
│   │   ├── metric.py
│   │   └── station.py
│   │
│   ├── services/
│   │   └── station_service.py
│   │
│   ├── main.py
│   └── __init__.py
│
├── requirements.txt
└── .env
Terminal Commands

Move into backend:

cd backend

Create files:

touch app/core/config.py

touch app/api/v1/api.py

touch app/api/v1/endpoints/health.py
touch app/api/v1/endpoints/stations.py
touch app/api/v1/endpoints/metrics.py

touch app/schemas/station.py
touch app/schemas/metric.py

touch app/services/station_service.py
File: backend/requirements.txt
fastapi==0.116.1
uvicorn[standard]==0.35.0

pydantic==2.11.7
pydantic-settings==2.10.1

python-dotenv==1.1.1
File: backend/.env
APP_NAME=SPACEOPS Monitoring Dashboard
API_V1_PREFIX=/api/v1
DEBUG=True
File: backend/app/core/config.py
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    APP_NAME: str
    API_V1_PREFIX: str
    DEBUG: bool = False

    class Config:
        env_file = ".env"


settings = Settings()
File: backend/app/schemas/station.py
from datetime import datetime

from pydantic import BaseModel


class Station(BaseModel):
    id: int
    station_name: str
    cpu_usage: float
    memory_usage: float
    signal_strength: float
    active_connections: int
    status: str
    last_telemetry_timestamp: datetime
File: backend/app/schemas/metric.py
from pydantic import BaseModel


class DashboardMetrics(BaseModel):
    total_stations: int
    online_stations: int
    offline_stations: int
File: backend/app/services/station_service.py
from datetime import datetime

from app.schemas.station import Station


stations = [
    Station(
        id=1,
        station_name="Bangalore Ground Station",
        cpu_usage=42.5,
        memory_usage=68.3,
        signal_strength=91.2,
        active_connections=18,
        status="ONLINE",
        last_telemetry_timestamp=datetime.utcnow(),
    ),
    Station(
        id=2,
        station_name="Hyderabad Ground Station",
        cpu_usage=35.1,
        memory_usage=55.8,
        signal_strength=88.7,
        active_connections=12,
        status="ONLINE",
        last_telemetry_timestamp=datetime.utcnow(),
    ),
    Station(
        id=3,
        station_name="Chennai Ground Station",
        cpu_usage=72.2,
        memory_usage=81.9,
        signal_strength=74.5,
        active_connections=7,
        status="OFFLINE",
        last_telemetry_timestamp=datetime.utcnow(),
    ),
]


def get_all_stations():
    return stations


def get_station_by_id(station_id: int):
    return next(
        (station for station in stations if station.id == station_id),
        None,
    )
File: backend/app/api/v1/endpoints/health.py
from fastapi import APIRouter

router = APIRouter()


@router.get("/health")
def health_check():
    return {
        "status": "healthy",
        "service": "spaceops-backend",
    }
File: backend/app/api/v1/endpoints/stations.py
from fastapi import APIRouter, HTTPException

from app.services.station_service import (
    get_all_stations,
    get_station_by_id,
)

router = APIRouter(prefix="/stations", tags=["Stations"])


@router.get("")
def get_stations():
    return get_all_stations()


@router.get("/{station_id}")
def get_station(station_id: int):
    station = get_station_by_id(station_id)

    if station is None:
        raise HTTPException(
            status_code=404,
            detail="Station not found",
        )

    return station
File: backend/app/api/v1/endpoints/metrics.py
from fastapi import APIRouter

from app.schemas.metric import DashboardMetrics
from app.services.station_service import get_all_stations

router = APIRouter(prefix="/metrics", tags=["Metrics"])


@router.get("", response_model=DashboardMetrics)
def get_metrics():
    stations = get_all_stations()

    online = len(
        [s for s in stations if s.status == "ONLINE"]
    )

    offline = len(
        [s for s in stations if s.status == "OFFLINE"]
    )

    return DashboardMetrics(
        total_stations=len(stations),
        online_stations=online,
        offline_stations=offline,
    )
File: backend/app/api/v1/api.py
from fastapi import APIRouter

from app.api.v1.endpoints.health import router as health_router
from app.api.v1.endpoints.metrics import router as metrics_router
from app.api.v1.endpoints.stations import router as stations_router

api_router = APIRouter()

api_router.include_router(health_router)
api_router.include_router(stations_router)
api_router.include_router(metrics_router)
File: backend/app/main.py
from fastapi import FastAPI

from app.api.v1.api import api_router
from app.core.config import settings

app = FastAPI(
    title=settings.APP_NAME,
    version="1.0.0",
)

app.include_router(
    api_router,
    prefix=settings.API_V1_PREFIX,
)


@app.get("/")
def root():
    return {
        "message": "SPACEOPS Monitoring Dashboard API",
    }
Install Dependencies

From the backend directory:

pip install -r requirements.txt
Run Application
uvicorn app.main:app --reload

Expected:

INFO: Uvicorn running on http://127.0.0.1:8000
Verification Steps
Root

Open:

http://127.0.0.1:8000/

Expected:

{
  "message": "SPACEOPS Monitoring Dashboard API"
}
Health
http://127.0.0.1:8000/api/v1/health

Expected:

{
  "status": "healthy",
  "service": "spaceops-backend"
}
Stations
http://127.0.0.1:8000/api/v1/stations

Should return 3 stations.

Single Station
http://127.0.0.1:8000/api/v1/stations/1

Should return Bangalore station.

Metrics
http://127.0.0.1:8000/api/v1/metrics

Expected:

{
  "total_stations": 3,
  "online_stations": 2,
  "offline_stations": 1
}
Swagger UI

Open:

http://127.0.0.1:8000/docs

You should see all endpoints listed and testable.

Stop here after implementing and testing Step 2.
Send me:

tree backend
any errors (if any)
output from /api/v1/stations

Then we'll move to STEP 3 — PostgreSQL Integration.

**step 3 ** 
STEP 3 — PostgreSQL & SQLAlchemy Foundation
Objective

Create the database foundation for the SPACEOPS Monitoring Dashboard.

By the end of this step:

FastAPI
   ↓
SQLAlchemy Engine
   ↓
Session Factory
   ↓
PostgreSQL Database

We are not creating tables yet.

We are only establishing:

Database configuration
SQLAlchemy setup
Session management
Database connectivity verification
1. Create Additional Files

From:

~/projects/spaceops-monitoring-dashboard/backend

Run:

touch app/db/database.py
touch app/db/base.py
2. Update requirements.txt

Replace the contents of:

backend/requirements.txt

with:

fastapi==0.116.1
uvicorn[standard]==0.35.0

pydantic==2.11.7
pydantic-settings==2.10.1

python-dotenv==1.1.1

sqlalchemy==2.0.42
psycopg2-binary==2.9.10

Install:

pip install -r requirements.txt

Expected:

Successfully installed sqlalchemy
Successfully installed psycopg2-binary
3. Update .env

Replace:

backend/.env

with:

APP_NAME=SPACEOPS Monitoring Dashboard
API_V1_PREFIX=/api/v1
DEBUG=True

POSTGRES_USER=spaceops
POSTGRES_PASSWORD=spaceops123
POSTGRES_DB=spaceops_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
4. Update Config

Replace:

backend/app/core/config.py

with:

from pydantic_settings import BaseSettings
from pydantic_settings import SettingsConfigDict


class Settings(BaseSettings):
    APP_NAME: str
    API_V1_PREFIX: str
    DEBUG: bool

    POSTGRES_USER: str
    POSTGRES_PASSWORD: str
    POSTGRES_DB: str
    POSTGRES_HOST: str
    POSTGRES_PORT: int

    model_config = SettingsConfigDict(
        env_file=".env",
        extra="ignore",
    )

    @property
    def database_url(self) -> str:
        return (
            f"postgresql://"
            f"{self.POSTGRES_USER}:"
            f"{self.POSTGRES_PASSWORD}@"
            f"{self.POSTGRES_HOST}:"
            f"{self.POSTGRES_PORT}/"
            f"{self.POSTGRES_DB}"
        )


settings = Settings()

Reason: This is the Pydantic v2 style and avoids deprecation warnings.

5. Create database.py

File:

backend/app/db/database.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from app.core.config import settings

engine = create_engine(
    settings.database_url,
    echo=settings.DEBUG,
)

SessionLocal = sessionmaker(
    autocommit=False,
    autoflush=False,
    bind=engine,
)
6. Create base.py

File:

backend/app/db/base.py
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass
7. Start PostgreSQL

Check service:

sudo service postgresql status

If not running:

sudo service postgresql start
8. Create Database & User

Open PostgreSQL:

sudo -u postgres psql

Inside PostgreSQL:

CREATE USER spaceops
WITH PASSWORD 'spaceops123';

CREATE DATABASE spaceops_db;

GRANT ALL PRIVILEGES
ON DATABASE spaceops_db
TO spaceops;

\q
9. Verify Database Exists

Run:

sudo -u postgres psql -l

Expected to see:

spaceops_db

in the database list.

10. Create Connection Test

Create:

backend/test_db.py

Contents:

from app.db.database import engine

print("Database Engine Created Successfully")
print(engine)
11. Run Connection Test
python test_db.py

Expected:

Database Engine Created Successfully
Engine(postgresql://spaceops:***@localhost:5432/spaceops_db)
Verification Checklist

You should have:

backend/
├── app/
│   ├── db/
│   │   ├── base.py
│   │   └── database.py
│   └── ...
├── .env
├── requirements.txt
└── test_db.py

And:

✅ PostgreSQL service running

✅ User spaceops created

✅ Database spaceops_db created

✅ SQLAlchemy installed

✅ psycopg2 installed

✅ Engine created successfully

Stop after completing Step 3.

Send me:

python test_db.py

output,

and:

sudo -u postgres psql -l

output.

Then we'll proceed to STEP 4 — Telemetry Model & Automatic Table Creation.

**STEP 4 **
TEP 4 — Telemetry Model & Automatic Table Creation
Objective

Create the first database table and configure FastAPI to create tables automatically when the application starts.

After this step:

PostgreSQL
   ↓
telemetry table
   ↓
FastAPI startup
   ↓
Auto table creation
Folder Structure After Step 4
backend/
├── app/
│   ├── db/
│   │   ├── base.py
│   │   └── database.py
│   │
│   ├── models/
│   │   └── telemetry.py
│   │
│   └── main.py
│
└── test_db.py
1. Create Telemetry Model

Create:

touch app/models/telemetry.py
File: app/models/telemetry.py
from datetime import datetime

from sqlalchemy import DateTime
from sqlalchemy import Float
from sqlalchemy import Integer
from sqlalchemy import String

from sqlalchemy.orm import Mapped
from sqlalchemy.orm import mapped_column

from app.db.base import Base


class Telemetry(Base):
    __tablename__ = "telemetry"

    id: Mapped[int] = mapped_column(
        Integer,
        primary_key=True,
        index=True,
    )

    station_name: Mapped[str] = mapped_column(
        String(100),
        nullable=False,
    )

    cpu_usage: Mapped[float] = mapped_column(
        Float,
        nullable=False,
    )

    memory_usage: Mapped[float] = mapped_column(
        Float,
        nullable=False,
    )

    signal_strength: Mapped[float] = mapped_column(
        Float,
        nullable=False,
    )

    active_connections: Mapped[int] = mapped_column(
        Integer,
        nullable=False,
    )

    status: Mapped[str] = mapped_column(
        String(20),
        nullable=False,
    )

    last_telemetry_timestamp: Mapped[datetime] = mapped_column(
        DateTime,
        nullable=False,
    )
2. Update base.py

Replace:

app/db/base.py

with:

from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass


from app.models.telemetry import Telemetry

__all__ = [
    "Base",
    "Telemetry",
]
3. Update main.py

Open:

app/main.py

Add the following imports near the top:

from app.db.base import Base
from app.db.database import engine

Add this startup function before the root endpoint:

@app.on_event("startup")
def startup_event():
    Base.metadata.create_all(bind=engine)

Your main.py should now contain:

from fastapi import FastAPI

from app.api.v1.api import api_router
from app.core.config import settings

from app.db.base import Base
from app.db.database import engine

app = FastAPI(
    title=settings.APP_NAME,
    version="1.0.0",
)


@app.on_event("startup")
def startup_event():
    Base.metadata.create_all(bind=engine)


app.include_router(
    api_router,
    prefix=settings.API_V1_PREFIX,
)


@app.get("/")
def root():
    return {
        "message": "SPACEOPS Monitoring Dashboard API",
    }
4. Start FastAPI

If it's already running, stop it:

CTRL + C

Start again:

uvicorn app.main:app --reload

Expected:

Application startup complete
5. Verify Table Creation

Open a new terminal.

Activate venv:

cd ~/projects/spaceops-monitoring-dashboard/backend

source .venv/bin/activate

Connect to PostgreSQL:

sudo -u postgres psql spaceops_db

Inside PostgreSQL:

\dt

Expected:

public | telemetry | table

Inspect table:

\d telemetry

Expected columns:

id
station_name
cpu_usage
memory_usage
signal_strength
active_connections
status
last_telemetry_timestamp

Exit:

\q
Verification Checklist

You should have:

✅ telemetry model created
✅ table auto-creation configured
✅ FastAPI startup creates tables
✅ telemetry table exists
Stop Here

Please send me:

\dt

output

and

\d telemetry

output.

Once that's confirmed, we'll move to:

STEP 5 — Mock Telemetry Generator

where we'll insert realistic telemetry records into PostgreSQL automatically instead of using hardcoded Python lists

**STEP 5 ** 

STEP 5 — Mock Telemetry Generator
Objective

Replace hardcoded Python lists with real telemetry records stored in PostgreSQL.

After this step:

FastAPI
   ↓
SQLAlchemy
   ↓
PostgreSQL
   ↓
Telemetry Records

Instead of:

stations = [...]

we'll generate and store telemetry data in the database.

New Structure
app/
├── telemetry/
│   └── seed_data.py
│
├── db/
│   ├── database.py
│   └── base.py
│
├── models/
│   └── telemetry.py
1. Create File
touch app/telemetry/seed_data.py
2. Create Seed Function
File: app/telemetry/seed_data.py
from datetime import datetime

from sqlalchemy.orm import Session

from app.models.telemetry import Telemetry


def seed_telemetry_data(db: Session):
    existing_records = db.query(Telemetry).count()

    if existing_records > 0:
        return

    stations = [
        Telemetry(
            station_name="Bangalore Ground Station",
            cpu_usage=42.5,
            memory_usage=68.3,
            signal_strength=91.2,
            active_connections=18,
            status="ONLINE",
            last_telemetry_timestamp=datetime.utcnow(),
        ),
        Telemetry(
            station_name="Hyderabad Ground Station",
            cpu_usage=35.1,
            memory_usage=55.8,
            signal_strength=88.7,
            active_connections=12,
            status="ONLINE",
            last_telemetry_timestamp=datetime.utcnow(),
        ),
        Telemetry(
            station_name="Chennai Ground Station",
            cpu_usage=72.2,
            memory_usage=81.9,
            signal_strength=74.5,
            active_connections=7,
            status="OFFLINE",
            last_telemetry_timestamp=datetime.utcnow(),
        ),
    ]

    db.add_all(stations)
    db.commit()
3. Add Database Dependency
Create file
touch app/db/session.py
File: app/db/session.py
from app.db.database import SessionLocal


def get_db():
    db = SessionLocal()

    try:
        yield db
    finally:
        db.close()
4. Update main.py

Add imports:

from app.db.database import SessionLocal
from app.telemetry.seed_data import seed_telemetry_data

Replace startup function with:

@app.on_event("startup")
def startup_event():
    Base.metadata.create_all(bind=engine)

    db = SessionLocal()

    try:
        seed_telemetry_data(db)
    finally:
        db.close()
5. Restart FastAPI

Stop server:

CTRL + C

Start again:

uvicorn app.main:app --reload

Expected:

Application startup complete
6. Verify Records Exist

Open PostgreSQL:

sudo -u postgres psql spaceops_db

Run:

SELECT * FROM telemetry;

Expected:

3 rows

with Bangalore, Hyderabad, and Chennai stations.

7. Count Records
SELECT COUNT(*) FROM telemetry;

Expected:

3
Verification Goal

At the end of Step 5:

✅ telemetry table exists
✅ telemetry table contains records
✅ startup automatically seeds data
✅ no duplicate data on restart
Stop Here

Run:

SELECT COUNT(*) FROM telemetry;

and:

SELECT station_name, status FROM telemetry;

Paste the output, and then we'll move to Step 6 — Replace the hardcoded service layer with real PostgreSQL queries.

** 
STEP 6 — Replace Hardcoded Service Layer with PostgreSQL Queries
Objective

Right now your API is still serving data from:

stations = [...]

inside:

app/services/station_service.py

We will now make the API fetch data directly from PostgreSQL.

After this step:

FastAPI
   ↓
API Endpoints
   ↓
Service Layer
   ↓
SQLAlchemy
   ↓
PostgreSQL
1. Create Response Schema

Update:

app/schemas/station.py

Replace entire file:

from datetime import datetime

from pydantic import BaseModel
from pydantic import ConfigDict


class Station(BaseModel):
    id: int
    station_name: str
    cpu_usage: float
    memory_usage: float
    signal_strength: float
    active_connections: int
    status: str
    last_telemetry_timestamp: datetime

    model_config = ConfigDict(
        from_attributes=True
    )

The from_attributes=True setting allows Pydantic to serialize SQLAlchemy objects.

2. Replace station_service.py

File:

app/services/station_service.py

Replace everything with:

from sqlalchemy.orm import Session

from app.models.telemetry import Telemetry


def get_all_stations(db: Session):
    return (
        db.query(Telemetry)
        .order_by(Telemetry.id)
        .all()
    )


def get_station_by_id(
    db: Session,
    station_id: int,
):
    return (
        db.query(Telemetry)
        .filter(Telemetry.id == station_id)
        .first()
    )
3. Update stations Endpoint

File:

app/api/v1/endpoints/stations.py

Replace entire file:

from typing import List

from fastapi import APIRouter
from fastapi import Depends
from fastapi import HTTPException

from sqlalchemy.orm import Session

from app.db.session import get_db

from app.schemas.station import Station

from app.services.station_service import (
    get_all_stations,
    get_station_by_id,
)

router = APIRouter(
    prefix="/stations",
    tags=["Stations"],
)


@router.get(
    "",
    response_model=List[Station],
)
def get_stations(
    db: Session = Depends(get_db),
):
    return get_all_stations(db)


@router.get(
    "/{station_id}",
    response_model=Station,
)
def get_station(
    station_id: int,
    db: Session = Depends(get_db),
):
    station = get_station_by_id(
        db,
        station_id,
    )

    if station is None:
        raise HTTPException(
            status_code=404,
            detail="Station not found",
        )

    return station
4. Update metrics Endpoint

File:

app/api/v1/endpoints/metrics.py

Replace:

from fastapi import APIRouter

with:

from fastapi import APIRouter
from fastapi import Depends

from sqlalchemy.orm import Session

Add imports:

from app.db.session import get_db

Replace the endpoint function with:

@router.get("", response_model=DashboardMetrics)
def get_metrics(
    db: Session = Depends(get_db),
):
    stations = get_all_stations(db)

    online = len(
        [
            s
            for s in stations
            if s.status == "ONLINE"
        ]
    )

    offline = len(
        [
            s
            for s in stations
            if s.status == "OFFLINE"
        ]
    )

    return DashboardMetrics(
        total_stations=len(stations),
        online_stations=online,
        offline_stations=offline,
    )
5. Restart FastAPI

Stop:

CTRL + C

Start:

uvicorn app.main:app --reload

Expected:

Application startup complete
6. Verification

Open:

http://127.0.0.1:8000/api/v1/stations

Expected:

[
  {
    "id": 1,
    "station_name": "Bangalore Ground Station",
    ...
  }
]

But now the records are coming from PostgreSQL, not a Python list.

Database Verification

Inside PostgreSQL:

INSERT INTO telemetry
(
 station_name,
 cpu_usage,
 memory_usage,
 signal_strength,
 active_connections,
 status,
 last_telemetry_timestamp
)
VALUES
(
 'Delhi Ground Station',
 61.5,
 74.2,
 89.4,
 14,
 'ONLINE',
 NOW()
);

Now call:

http://127.0.0.1:8000/api/v1/stations

You should see:

Delhi Ground Station

appear automatically.

That proves the API is reading from PostgreSQL.

Verification Checklist
✅ Hardcoded station list removed
✅ API reads from PostgreSQL
✅ SQLAlchemy session dependency works
✅ Endpoints use database records
Stop after Step 6

Send me:

SELECT COUNT(*) FROM telemetry;

and the output from:

http://127.0.0.1:8000/api/v1/stations

Then we'll move to STEP 7 — Realistic Dynamic Telemetry Generator (random CPU, memory, signal updates).

**STEP 7 ** 
STEP 6 COMPLETE ✅

Current project status:

✅ Step 1 - Project Structure
✅ Step 2 - FastAPI Foundation
✅ Step 3 - PostgreSQL + SQLAlchemy Foundation
✅ Step 4 - Telemetry Model + Table Creation
✅ Step 5 - Database Seeding
✅ Step 6 - API Reads from PostgreSQL

STEP 7 — Dynamic Telemetry Simulator
Objective

Instead of static values:

CPU = 42.5
Memory = 68.3
Signal = 91.2

we will automatically generate changing telemetry values.

Every server startup will update the stations with realistic values.

After this step:

Database
   ↓
Telemetry Simulator
   ↓
Updated Metrics
   ↓
API
New Structure
app/
├── telemetry/
│   ├── seed_data.py
│   └── simulator.py
1. Create Simulator File

Run:

touch app/telemetry/simulator.py
2. Create Simulator
File: app/telemetry/simulator.py
import random
from datetime import datetime

from sqlalchemy.orm import Session

from app.models.telemetry import Telemetry


def update_telemetry_data(db: Session):
    stations = db.query(Telemetry).all()

    for station in stations:
        station.cpu_usage = round(
            random.uniform(15, 95),
            2,
        )

        station.memory_usage = round(
            random.uniform(20, 90),
            2,
        )

        station.signal_strength = round(
            random.uniform(60, 100),
            2,
        )

        station.active_connections = random.randint(
            5,
            50,
        )

        station.status = random.choice(
            [
                "ONLINE",
                "ONLINE",
                "ONLINE",
                "OFFLINE",
            ]
        )

        station.last_telemetry_timestamp = datetime.utcnow()

    db.commit()
3. Update main.py

Add import:

from app.telemetry.simulator import update_telemetry_data

Update startup function:

@app.on_event("startup")
def startup_event():
    Base.metadata.create_all(bind=engine)

    db = SessionLocal()

    try:
        seed_telemetry_data(db)
        update_telemetry_data(db)
    finally:
        db.close()
4. Restart Server
CTRL + C

Start again:

uvicorn app.main:app --reload

Expected:

Application startup complete
5. Verify Dynamic Updates

Open:

http://127.0.0.1:8000/api/v1/stations

Note the values.

Restart:

CTRL + C

uvicorn app.main:app --reload

Open again:

http://127.0.0.1:8000/api/v1/stations

Expected:

CPU changes
Memory changes
Signal changes
Connections change
Timestamp changes
6. Database Verification

Run:

SELECT
station_name,
cpu_usage,
memory_usage,
signal_strength,
active_connections,
status
FROM telemetry;

You should see realistic values similar to:

Bangalore Ground Station | 74.12 | 52.40 | 96.51 | 22 | ONLINE
Hyderabad Ground Station | 31.44 | 81.20 | 72.15 | 14 | ONLINE
Chennai Ground Station   | 88.91 | 63.55 | 67.40 | 40 | OFFLINE
Verification Checklist
✅ Dynamic telemetry generation
✅ Realistic CPU values
✅ Realistic memory values
✅ Realistic signal values
✅ Database updates
✅ API returns updated data
Stop Here

After restarting the server and checking /api/v1/stations, send me:

SELECT
station_name,
cpu_usage,
memory_usage,
signal_strength,
active_connections,
status
FROM telemetry;

output.

Then we'll move to Step 8 — Angular Frontend Foundation.

**STEP 8 — Angular Frontend Foundation
Objective

Create the Angular application that will consume the FastAPI backend.

After this step:

frontend/
   ↓
Angular
   ↓
Angular Material
   ↓
HTTP Client
   ↓
Ready for Dashboard UI
Verify Node.js

Run:

node --version
npm --version

Expected:

v20.x.x (or newer)
10.x.x (or newer)
Verify Angular CLI

Run:

ng version

If Angular CLI is not installed:

npm install -g @angular/cli

Verify again:

ng version
Create Angular Application

From the project root:

cd ~/projects/spaceops-monitoring-dashboard/frontend

Create Angular app in the current directory:

ng new . \
--routing \
--style=scss \
--skip-git \
--standalone

Choose:

Would you like to enable SSR and SSG? → No
Install Angular Material

Inside frontend:

ng add @angular/material

Choose:

Theme:
Indigo/Pink

Typography:
Yes

Animations:
Yes
Verify Structure

You should now have:

frontend/
├── src/
├── public/
├── angular.json
├── package.json
├── tsconfig.json
└── ...
Run Angular
npm install
ng serve

Expected:

Application bundle generation complete

Open:

http://localhost:4200

Expected Angular welcome page.

Verification Checklist
✅ Angular CLI installed
✅ Angular project created
✅ Angular Material installed
✅ npm packages installed
✅ Angular app running on localhost:4200
Stop Here

Please send:

node --version
npm --version
ng version

and the output from:

ls frontend

(or a screenshot/text showing Angular running on http://localhost:4200).

Once Angular is verified, we'll proceed to:

Step 9 — Angular Project Architecture (core, shared, services, models, dashboard feature modules).

STEP 9 — Angular Project Architecture
Objective

Create the production-ready Angular folder structure for the SpaceOps Monitoring Dashboard.

We are not connecting to the backend yet.

This step only creates the architecture.

Target Structure

After this step:

src/app/
├── core/
│   └── layout/
│
├── shared/
│
├── features/
│   └── dashboard/
│       ├── pages/
│       ├── components/
│       └── services/
│
├── models/
│
├── services/
│
├── app.component.ts
├── app.component.html
├── app.component.scss
├── app.config.ts
└── app.routes.ts
Create Directories

Open a new terminal.

Keep the Angular server running in the first terminal.

In a second terminal:

cd ~/projects/spaceops-monitoring-dashboard/frontend

Run:

mkdir -p src/app/core/layout
mkdir -p src/app/shared
mkdir -p src/app/features/dashboard/pages
mkdir -p src/app/features/dashboard/components
mkdir -p src/app/features/dashboard/services
mkdir -p src/app/models
mkdir -p src/app/services
Verification

Run:

find src/app -type d | sort
Expected Output

Something similar to:

src/app
src/app/core
src/app/core/layout
src/app/features
src/app/features/dashboard
src/app/features/dashboard/components
src/app/features/dashboard/pages
src/app/features/dashboard/services
src/app/models
src/app/services
src/app/shared
Verification Required

Send me the output of:

find src/app -type d | sort

Once verified, we'll create the first dashboard page component and wire up Angular routing. We won't touch backend integration until the architecture is confirmed.
STEP 10 — Create Dashboard Page Component
Objective

Create the first feature page:

Dashboard Page

This will become the main screen of the SpaceOps Monitoring Dashboard.

For this step we will:

Create a Dashboard Page component
Configure Angular routing
Display a simple page title
Verify routing works

We are not connecting to FastAPI yet.

Folder Structure After This Step
src/app/features/dashboard/pages/
└── dashboard-page/
    ├── dashboard-page.component.ts
    ├── dashboard-page.component.html
    └── dashboard-page.component.scss
Create Component

From:

cd ~/projects/spaceops-monitoring-dashboard/frontend

Run:

ng generate component features/dashboard/pages/dashboard-page --skip-tests

Expected:

CREATE src/app/features/dashboard/pages/dashboard-page/dashboard-page.component.ts
CREATE src/app/features/dashboard/pages/dashboard-page/dashboard-page.component.html
CREATE src/app/features/dashboard/pages/dashboard-page/dashboard-page.component.scss
Update Dashboard HTML
File
src/app/features/dashboard/pages/dashboard-page/dashboard-page.component.html

Replace everything with:

<h1>SpaceOps Monitoring Dashboard</h1>

<p>Angular frontend is running successfully.</p>
Update Routes
File
src/app/app.routes.ts

Replace the entire file with:

import { Routes } from '@angular/router';

import { DashboardPageComponent } from './features/dashboard/pages/dashboard-page/dashboard-page.component';

export const routes: Routes = [
  {
    path: '',
    component: DashboardPageComponent,
  },
];
Update App Component HTML
File
src/app/app.component.html

Replace the entire file with:

<router-outlet></router-outlet>
Update App Component TS
File
src/app/app.component.ts

Replace the entire file with:

import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  imports: [RouterOutlet],
  templateUrl: './app.component.html',
  styleUrl: './app.component.scss',
})
export class AppComponent {}
Expected Browser Result

The Angular dev server should automatically reload.

Open:

http://localhost:4200

Expected page:

SpaceOps Monitoring Dashboard

Angular frontend is running successfully.
Verification Required

Send me:

Component generation output
ng generate component features/dashboard/pages/dashboard-page --skip-tests
Browser result

Tell me what appears at:

http://localhost:4200

Once verified, we'll move to creating TypeScript models for telemetry data.
STEP 11 — Create TypeScript Models
Objective

Create strongly typed models that match the FastAPI backend responses.

We are still not calling the backend yet.

This step only creates the data contracts.

Folder
src/app/models/
Create File 1
File
src/app/models/station.model.ts
Code
export interface Station {
  id: number;
  station_name: string;
  cpu_usage: number;
  memory_usage: number;
  signal_strength: number;
  active_connections: number;
  status: string;
  last_telemetry_timestamp: string;
}
Create File 2
File
src/app/models/dashboard-metrics.model.ts
Code
export interface DashboardMetrics {
  total_stations: number;
  online_stations: number;
  offline_stations: number;
}
Expected Structure
src/app/models/
├── station.model.ts
└── dashboard-metrics.model.ts
Verification

Run:

find src/app/models -type f | sort
Expected Output
src/app/models/dashboard-metrics.model.ts
src/app/models/station.model.ts
Verification Required

Send me the output of:

find src/app/models -type f | sort

Once verified, we'll create the Angular API service layer that connects to your FastAPI endpoints:

GET /api/v1/stations
GET /api/v1/stations/{id}
GET /api/v1/metrics

One step at a time.

STEP 12 — Create API Service Layer
Objective

Create a reusable Angular service that will communicate with FastAPI.

We are creating the service first.

We are not testing backend connectivity yet.

Folder
src/app/services
Create Service
File
src/app/services/api.service.ts
Code
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';

import { Observable } from 'rxjs';

import { Station } from '../models/station.model';
import { DashboardMetrics } from '../models/dashboard-metrics.model';

@Injectable({
  providedIn: 'root',
})
export class ApiService {
  private readonly apiUrl = 'http://localhost:8000/api/v1';

  constructor(
    private http: HttpClient,
  ) {}

  getStations(): Observable<Station[]> {
    return this.http.get<Station[]>(
      `${this.apiUrl}/stations`
    );
  }

  getStationById(
    id: number,
  ): Observable<Station> {
    return this.http.get<Station>(
      `${this.apiUrl}/stations/${id}`
    );
  }

  getMetrics(): Observable<DashboardMetrics> {
    return this.http.get<DashboardMetrics>(
      `${this.apiUrl}/metrics`
    );
  }
}
Register HttpClient

Angular standalone apps require HttpClient registration.

File
src/app/app.config.ts

Replace entire file with:

import { ApplicationConfig } from '@angular/core';

import {
  provideRouter,
} from '@angular/router';

import {
  provideHttpClient,
} from '@angular/common/http';

import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes),
    provideHttpClient(),
  ],
};
Expected Structure
src/app/services/
└── api.service.ts
Verification

Run:

find src/app/services -type f | sort

Expected:

src/app/services/api.service.ts
Verification Required

Send me:

find src/app/services -type f | sort

Also tell me whether Angular recompiles successfully after updating:

app.config.ts

Do not move ahead to backend integration yet. We verify the service layer firs